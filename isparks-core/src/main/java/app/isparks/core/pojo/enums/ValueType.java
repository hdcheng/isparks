package app.isparks.core.pojo.enums;

/**
 * 值数据类型
 *
 * @author chenghd
 * @date 2020/7/24
 */
public enum ValueType {
    STRING, INTEGER, LONG, FLOAT, DOUBLE, BOOLEAN, CHAR, BYTE, SHORT,
    /**
     * 类，对象
     */
    CLASS;
}
